# نصب روی ویندوز — نوین پرداز نسل بعد

دو راه برای دیدن نرم‌افزار وجود دارد. راه اول در چند ثانیه، راه دوم یک
برنامه‌ی واقعی نصب‌شدنی می‌دهد.

---

## راه ۱ — دیدن فوری بدون نصب (چند ثانیه)

**همه‌ی دستورها از ریشه‌ی مخزن اجرا می‌شوند** — لازم نیست وارد پوشه‌ی
دیگری شوید.

```
npm run setup
npm run dev
```

مرورگر را روی نشانی‌ای که نمایش داده می‌شود باز کنید. برنامه با
**داده‌ی نمونه‌ی کامل** بالا می‌آید و همه‌ی صفحه‌ها قابل گشتن‌اند.

> این حالت فقط برای بازبینی است؛ داده‌ای ذخیره نمی‌شود.

### فهرست کامل دستورها

| دستور | کار |
|---|---|
| `npm run setup` | نصب وابستگی‌ها |
| `npm run dev` | پیش‌نمایش در مرورگر با داده‌ی نمونه |
| `npm test` | اجرای تست‌ها |
| `npm run typecheck` | بررسی نوع‌ها |
| `npm run check` | بررسی نوع‌ها + تست‌ها با هم |
| `npm run build` | ساخت نسخه‌ی تولیدی رابط کاربری |
| `npm run desktop` | اجرای برنامه‌ی دسکتاپ (نیازمند Rust) |
| `npm run installer` | ساخت فایل نصبی ویندوز |
| `npm run core:test` | تست‌های هسته‌ی مالی (نیازمند Rust) |
| `npm run core:lint` | بررسی کیفیت کد هسته |

---

## راه ۲ — ساخت نصاب واقعی ویندوز (یک کلیک)

روی فایل زیر دوبار کلیک کنید:

```
BUILD_WINDOWS_INSTALLER.cmd
```

یا از خط فرمان، در ریشه‌ی مخزن:

```
npm run installer
```

اسکریپت خودش:

1. بررسی می‌کند Node.js، Rust و ابزار کامپایل C++ مایکروسافت نصب باشند —
   و اگر نبودند با `winget` نصب‌شان می‌کند.
2. وابستگی‌های رابط کاربری را نصب و نوع‌ها را بررسی می‌کند.
3. رابط کاربری را می‌سازد.
4. برنامه‌ی دسکتاپ را کامپایل و **فایل نصاب** را تولید می‌کند.
5. پوشه‌ی خروجی را باز می‌کند.

### خروجی کجاست؟

```
target\release\bundle\nsis\   →  نصاب ساده (.exe)   ← این را اجرا کنید
target\release\bundle\msi\    →  نصاب سازمانی (.msi)
```

### چقدر طول می‌کشد؟

- **بار اول:** ۱۰ تا ۲۵ دقیقه (کامپایل کامل Rust و دریافت وابستگی‌ها)
- **دفعات بعد:** ۲ تا ۴ دقیقه (کش Cargo استفاده می‌شود)

### پیش‌نیازها اگر خواستید دستی نصب کنید

| ابزار | نشانی |
|---|---|
| Node.js LTS | <https://nodejs.org/en/download> |
| Rust | <https://rustup.rs> |
| Visual C++ Build Tools | <https://visualstudio.microsoft.com/visual-cpp-build-tools/> |

هنگام نصب Build Tools حتماً بسته‌ی **Desktop development with C++** را
انتخاب کنید.

---

## داده‌ی نمونه‌ی داخل برنامه

نصاب با **حالت نمایشی روشن** ساخته می‌شود: برنامه خودکار وارد می‌شود
(`admin` / `demo`) و پایگاه داده از پیش پر است. آنچه می‌بینید:

| بخش | محتوای نمونه |
|---|---|
| کالاها | ۶۰ کالا در ۵ گروه، با ۷ سطح قیمت و پله‌های تخفیف |
| اشخاص | ۵۰ شخص حقیقی و حقوقی، با مانده‌ی بدهکار/بستانکار/تسویه |
| فروش | ۵۵ فاکتور فروش با سطر، سند حسابداری و گردش انبار |
| خرید | ۲۵ فاکتور خرید |
| برگشت | ۸ برگشت از فروش و ۳ برگشت از خرید |
| پیش‌فاکتور | ۱۸ پیش‌فاکتور و سفارش خرید در پنج وضعیت |
| چک | ۲۰ چک دریافتی و پرداختی در وضعیت‌های مختلف چرخه |
| خزانه | ۵ صندوق و بانک با ۲۴ سند دریافت و پرداخت چندروشی |
| انبار | ۵ انبار، موجودی واقعی، ۱۰ انتقال بین انبارها، ۶ سری/سریال |
| تولید | ۲ فرمول ساخت و ۲ رسید تولید با بهای تمام‌شده‌ی متوازن |
| انبارگردانی | یک دوره‌ی در حال شمارش با اختلاف واقعی برای تمرین چرخه |
| گزارش‌ها | ۳ گزارش ذخیره‌شده در گزارش‌ساز |
| قالب چاپ | ۴ قالب: فاکتور A4، رسید حرارتی، سند، برچسب |
| اتصالات | ۳ اتصال API و ۲ افزونه‌ی نمونه |

### پاک کردن داده‌ی نمونه

مرکز تنظیمات ← عملیات مدیریتی ← «حذف داده‌های نمونه». فقط رکوردهایی که
با پیشوند نمونه ساخته شده‌اند حذف می‌شوند.

### محل ذخیره‌ی اطلاعات

پایگاه داده‌ی SQLite در پوشه‌ی داده‌ی برنامه‌ی کاربر ویندوز ساخته می‌شود؛
حذف برنامه آن را پاک نمی‌کند.

---

## نکته درباره‌ی نسخه‌ی تجاری

حالت نمایشی برای ارزیابی است. در نسخه‌ی تجاری باید
`VITE_DEMO_MODE=false` باشد و صفحه‌ی ورود واقعی جایگزین ورود خودکار شود.
